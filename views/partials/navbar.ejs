<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">PrivateWallet</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
            <div class="navbar-nav">
                <% if(currentUser){ %>
                    <a class="nav-link" href="/">ABOUT</a>
                    <a class="nav-link" href="/<%=currentUser._id %>">DASH BOARD</a>
                    <a class="nav-link" href="/<%=currentUser._id %>/data">UPDATE TODAY'S AMOUNT</a>
                    <a class="nav-link" href="/<%=currentUser._id %>/edit">EDIT ACCOUNT</a>
                    <% } else {%>
                        <a class="nav-link" href="/">ABOUT</a>
                        <% } %>
            </div>
            <div class="navbar-nav ms-auto">
                <% if(!currentUser){ %>
                    <a class="nav-link" href="/login">LOGIN</a>
                    <a class="nav-link" href="/register">SIGN UP</a>
                    <% } else {%>
                        <a class="nav-link" href="/logout">LOGOUT</a>
                        <% } %>
            </div>
        </div>
    </div>
</nav>